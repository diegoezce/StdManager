{
  "$schema": "https://raw.githubusercontent.com/railwayapp/cli/main/assets/config.schema.json",
  "build": {
    "builder": "dockerfile",
    "context": "."
  },
  "deploy": {
    "startCommand": "sh -c 'cd backend && python manage.py migrate && gunicorn config.wsgi:application --bind 0.0.0.0:$PORT'",
    "restartPolicyMaxRetries": 5,
    "restartPolicyWindowSeconds": 60
  }
}
